# Инструкция по первому запуску

## Шаг 1: Установка Python

Убедитесь, что у вас установлен Python 3.11 или выше:

```bash
python --version
```

## Шаг 2: Создание виртуального окружения

```bash
# Windows PowerShell
python -m venv venv
.\venv\Scripts\Activate.ps1

# Если возникает ошибка политики выполнения:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 4: Создание Telegram-бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Сохраните токен — он понадобится в `.env`

## Шаг 5: Получение ID группы

1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в вашу группу
2. Бот отправит ID группы (например: `-1001234567890`)
3. Удалите бота из группы после получения ID

## Шаг 6: Получение вашего Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Отправьте любое сообщение
3. Бот отправит ваш ID (например: `123456789`)

## Шаг 7: Настройка .env

Скопируйте `.env.example` в `.env`:

```bash
copy .env.example .env
```

Откройте `.env` в текстовом редакторе и заполните:

```env
# Авторизация на сайте MangaBuff
LOGIN_EMAIL=ваш_email@example.com
LOGIN_PASSWORD=ваш_пароль

# Telegram
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_TG_ID=123456789
REQUIRED_TG_GROUP_ID=-1001234567890
```

## Шаг 8: Подготовка шаблонов рангов

### Вариант 1: Автоматическое скачивание (рекомендуется)

Создайте папку `ranks/` и добавьте туда скриншоты карт разных рангов:

1. Откройте страницу карты на MangaBuff
2. Сделайте скриншот карты (размер 288×432 пикселей)
3. Сохраните как `frame-{ранг}.png` (например: `frame-e.png`, `frame-d.png`)

### Вариант 2: Ручное создание

Если у вас уже есть изображения карт:

```
ranks/
├── frame-e.png    # Ранг E
├── frame-d.png    # Ранг D
├── frame-c.png    # Ранг C
├── frame-b.png    # Ранг B
├── frame-a.png    # Ранг A
└── frame-s.png    # Ранг S
```

**Важно:** Изображения должны быть размером 288×432 пикселей!

## Шаг 9: Первый запуск

```bash
python main.py
```

Вы должны увидеть:

```
🚀 Запуск бота мониторинга клуба MangaBuff
✅ База данных инициализирована
✅ Прокси-менеджер инициализирован
🔐 Авторизация на сайте...
✅ Авторизация успешна
✅ Детектор рангов готов: 6 шаблонов для рангов ['A', 'B', 'C', 'D', 'E', 'S']
🤖 Инициализация Telegram-бота...
✅ Обработчики зарегистрированы
✅ Планировщик броней запущен
✅ Бот запущен и готов к работе
🔄 Запуск фонового парсера...
✅ ВСЕ СИСТЕМЫ ЗАПУЩЕНЫ
```

## Шаг 10: Проверка работы

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Следуйте инструкциям для привязки аккаунта

## Возможные проблемы

### Ошибка авторизации на сайте

```
❌ Не удалось авторизоваться на сайте
```

**Решение:**
- Проверьте правильность email и пароля в `.env`
- Убедитесь, что аккаунт активен на MangaBuff

### Ошибка прокси

```
⚠️  Не удалось найти рабочий прокси
```

**Решение:**
- Прокси ищутся автоматически из RU/UA/BY
- Попробуйте перезапустить бота
- Если проблема сохраняется, отключите прокси в `proxy_manager.py`:
  ```python
  proxy_manager = ProxyManager(enabled=False)
  ```

### Детектор рангов не готов

```
⚠️  Детектор рангов не готов (нет шаблонов)
```

**Решение:**
- Убедитесь, что папка `ranks/` существует
- Добавьте хотя бы один файл `frame-*.png`
- Проверьте размер изображений (должен быть 288×432)

### Ошибка импорта модулей

```
ModuleNotFoundError: No module named 'telegram'
```

**Решение:**
```bash
pip install -r requirements.txt
```

### Ошибка политики выполнения (Windows)

```
cannot be loaded because running scripts is disabled
```

**Решение:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

## Остановка бота

Нажмите `Ctrl+C` в терминале. Бот корректно завершит работу:

```
⏹ Получен сигнал остановки
⏹ Планировщик остановлен
⏹ Бот остановлен
⏹ Сессия закрыта
👋 Бот завершил работу
```

## Запуск в фоновом режиме (опционально)

### Windows (с помощью pythonw)

```bash
pythonw main.py
```

### Linux/Mac (с помощью screen)

```bash
screen -S mangabuff_bot
python main.py
# Нажмите Ctrl+A, затем D для отсоединения
# Для возврата: screen -r mangabuff_bot
```

## Обновление бота

```bash
git pull
pip install -r requirements.txt --upgrade
python main.py
```

## Поддержка

При возникновении проблем:

1. Проверьте логи в файле `bot.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность настроек в `.env`

## Полезные ссылки

- [Документация python-telegram-bot](https://docs.python-telegram-bot.org/)
- [MangaBuff](https://mangabuff.ru)
- [Техническое задание](tz_mangabuff_bot.md)
